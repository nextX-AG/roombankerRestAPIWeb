{
  "events": [
    {
      "message": "{% if alarm_status == 'alarm' %}ALARM{% else %}Status{% endif %}: {{ device_id }} - {{ gateway_id }}",
      "address": "0",
      "namespace": "{{ namespace }}",
      "id": "{{ uuid }}",
      "device_id": "{{ device_id }}"
    }
  ]
}
