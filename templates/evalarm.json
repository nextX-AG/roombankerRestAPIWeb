{
  "transform": {
    "events": [
      {
        "message": "Alarm von Gateway {{ gateway_id }}",
        "namespace": "{{ customer_config.evalarm_namespace if customer_config else 'default' }}",
        "id": "{{ uuid }}",
        "timestamp": "{{ timestamp }}",
        "gateway_id": "{{ gateway_id }}",
        "raw_message": "{{ message }}"
      }
    ]
  }
}
